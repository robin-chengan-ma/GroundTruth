---
title: Phase 5 安全與應用切換修復紀錄
updated: 2026-09-02
---

# Phase 5 安全與應用切換修復紀錄

## 2026-08-31 n8n v2 候選解析繞過敏感資料遮罩
**現象**：新版 `inquiries/parse/` 雖只回傳候選資料且不建單，但 Django 將使用者 `raw_text` 原文直接送到 n8n／Gemini；多間供應商名稱與具金額語境的數字未經 Phase 3 固定遮罩。
**排查過程**：沿 `InquiryCandidateParseView → parse_purchase_request_candidate() → request_candidate_parse()` 檢查資料流，確認 Service 未呼叫 masking service，Repository 直接以原文建立 webhook payload。另確認既有 `mask_text()` 專為 legacy 單供應商流程設計，兩間完整命中的供應商會被視為歧義，不能直接重用於新版多候選流程。
**根因**：D2 建立新候選解析契約時只分離「AI 候選」與「正式建單」，沒有把 FR-2／NFR-1 的遮罩邊界一併移植到新 webhook；因此形成新舊流程安全能力不一致。
**修復方式**：在 `backend/services/masking_service.py` 新增新版專用 `mask_candidate_text()`，將所有完整命中的已建檔供應商與金額 Token 化；顯式供應商片段混有未知名稱時採 fail closed，完全未知沿用查無分流，模糊命中沿用人工複核。新增 `unmask_payload()` 遞迴還原 n8n 回傳的巢狀候選字串。`backend/services/inquiry_service.py` 改為遮罩成功後才呼叫 Repository，mapping 只留在單次函式呼叫記憶體。
**驗證方式**：TDD RED 確認多供應商／金額仍以原文送出及遞迴還原函式不存在；GREEN 驗證單一與多供應商、金額、巢狀 payload、未知供應商、混合已知／未知、空值、Malformed payload 與 API 權限。目標測試、完整回歸、coverage、Ruff、Django check、Migration check 於 PROGRESS 記錄最終結果。
**未驗證範圍**：無。Robin 已以真實 n8n production webhook 從 Vue 驗證兩間供應商、兩品項與預算情境：LLM Input 僅含 `SUP_001`／`SUP_002`／`AMOUNT_001`，不含真實供應商名稱與金額；LLM Output 保留供應商 Token，Django 正確還原兩間候選供應商。workflow JSON 依 ignore／敏感資料規則不由 AI 讀取或修改。

## 2026-09-01 接手中斷的 Phase 5：查詢契約缺口與撤回狀態機 bug
**現象**：接手前一個 AI 工具中斷的 Phase 5 工作前，先建立基線盤點，發現三類問題：①`InventoryViewSet` 用 `AuthenticatedReadAdminWrite`（等同任何 `master_data.read` 的人都能查庫存），與庫存查詢應有專屬權限的設計不一致；②`ApprovalStep` 的 `claim`／`decide` 對「需求已被撤回」的案件沒有狀態防線；③撰寫 ②的 regression test 時（`test_claim_step_rejected_after_request_withdrawn_cancels_case`），測試在到達預期的 `ApprovalWorkflowConflict` 之前就先炸出 `django.db.utils.IntegrityError: ... violates check constraint "award_decisions_submission_time_consistent"`。
**排查過程**：①②為唯讀盤點直接發現，非除錯。③一開始懷疑是測試 fixture 沒有正確把 `AwardDecision` 提交（`submitted_at` 為空）才觸發 constraint，但追蹤 `purchase_request_draft_service.withdraw_request()` 的實際邏輯後確認：任何一筆「已進入 AWARDING 狀態、但得標決議尚未走完提交流程」的真實採購需求被撤回時，都會呼叫同一段「取消得標決議」的 `.update()`，同樣會在正式環境撞上這個 DB constraint——不是測試資料不真實，是測試資料照實還原了一個目前生產程式碼本來就會炸掉的路徑。
**根因**：①`InventoryViewSet` 建立時圖方便沿用了主資料的粗粒度權限類別，未依庫存查詢的實際敏感度另外分權限碼。②`claim_step`／`decide_step` 只檢查關卡本身狀態，沒有檢查其所屬 `ApprovalCase`／需求是否仍在有效流程中。③`withdraw_request` 取消得標決議時直接把 `status` 設為 `cancelled` 並 `.save()`，但 `AwardDecision` 的 DB CHECK constraint 要求「非 draft 狀態時 `submitted_at` 不得為空」，撤回邏輯完全沒考慮「得標決議還沒提交」這個中間狀態。
**修復方式**：①`backend/api/erp/views.py`：`InventoryViewSet` 改用 `HasPermissionCode`＋`required_permission = "inventory.read"`；同步在 `admin_user` fixture（`tests/conftest.py`）與 `seed_demo_data.py` 補上這個新權限碼。②`backend/services/approval_case_service.py` 新增 `_ACTIVE_CASE_STATUSES`／`_ensure_case_active(case)`，在 `claim_step`／`decide_step` 內以 `select_for_update()` 先鎖定 case 再檢查，不符合時拋 `ApprovalWorkflowConflict`（API 層回 409）。③`backend/services/purchase_request_draft_service.py`：取消得標決議前用 `Coalesce(F("submitted_at"), Value(timezone.now()))` 補齊送出時間，一次 `.update()` 內同時補時間與轉態，不覆寫既有值。
**驗證方式**：新增 6 個測試（含 1 個鎖定 constraint bug 的專屬 regression test）。TDD RED 確認①未授權查庫存會通過、②撤回後仍可認領／決議、③撤回會噴 IntegrityError；GREEN 全部通過。完整回歸 Backend 349→354 passed，Ruff、`makemigrations --check --dry-run`、`manage.py check` 通過，已用 SHA-256 雙邊校驗同步回裝置（見 PROGRESS.md 對應列）。
**未驗證範圍**：Robin 尚未在真實瀏覽器／n8n 環境重新走一次庫存查詢與撤回流程；僅完成後端自動測試驗證。

## 2026-09-01 RFQ／供應商報價／得標決議缺少查詢契約，供應商品項與版本價格完全沒有 API
**現象**：延續同一次基線盤點，發現 `RfqViewSet`／`SupplierQuoteViewSet`／`AwardDecisionViewSet` 三個 ViewSet 完全沒有定義 `list`／`retrieve` 方法；DRF `DefaultRouter` 只會依 ViewSet 實際定義的方法產生對應路由，因此 `GET /api/v1/rfqs/`、`GET /api/v1/supplier-quotes/`、`GET /api/v1/award-decisions/` 及其 `{id}/` 版本並非回 404，而是路由本身就不存在。另外盤點 `apps/procurement/models.py` 發現 `SupplierProduct`／`SupplierPriceVersion`（供應商可供應品項與版本化價格）已有完整 Model、DB constraint 與既有 `repositories/procurement.py` 內部查價邏輯依賴這兩個 Model，但完全沒有對外 REST API——沒有任何 endpoint 可以建立、查詢、啟用／停用供應商品項關係或新增價格版本。
**排查過程**：唯讀盤點 `api/procurement/views.py`、`api/procurement/urls.py` 與 `apps/procurement/models.py` 直接發現，非執行期錯誤觸發的除錯。
**根因**：這兩類缺口都源自先前的開發流程只從「業務動作」（送出報價、決標、簽核）角度補齊 command API，沒有從「SPEC.md 明列的查詢契約清單」逐項核對，導致三個資源的查詢路由與供應商品項這整個資源都被遺漏。
**修復方式**：①新增 `RfqRepository.accessible()`／`SupplierQuoteRepository.accessible()`／`AwardRepository.accessible()`（皆含對應 `select_related`／`prefetch_related`），在 `rfq_quote_service.py`／`award_selection_service.py` 補上 `_require_*_read_permission`／`list_accessible_*`／`get_accessible_*`，並在對應 ViewSet 補上 `list`／`retrieve`。②新增 `SupplierProductRepository`（含新增價格版本前的時間重疊查詢 `overlapping_price_versions()`）、新檔 `services/supplier_product_service.py`（`master_data.read`／`master_data.manage` 權限分流；主檔只能啟用／停用不得實體刪除，回 409／`physical_delete_forbidden`；價格版本只能新增不得覆寫既有版本，新增前檢查同幣別／數量級距是否已有時間重疊的有效版本，重疊回 409）、`schemas/procurement.py` 新增對應 Serializer、`api/procurement/views.py` 新增 `SupplierProductViewSet`（含 `POST {id}/price-versions/` action）並在 `urls.py` 註冊 `supplier-products` 路由。
**驗證方式**：查詢契約補齊新增 11 個測試（`tests/test_phase5_query_contracts.py`），供應商品項 CRUD 新增 6 個測試（`tests/test_phase5_supplier_product_crud.py`），皆先 RED 後 GREEN。完整回歸 Backend 354→371 passed，Ruff（自動修正 3 個 `FURB157` 冗長 `Decimal()` 寫法後重跑確認仍全過）、`makemigrations --check --dry-run`（No changes detected）、`manage.py check`（0 issues）通過，已用 SHA-256 雙邊校驗同步回裝置（見 PROGRESS.md 對應列）。
**未驗證範圍**：Robin 尚未在真實瀏覽器環境操作供應商品項與價格版本維護（目前無對應前端頁面，屬 Phase 6 範圍）；`QuoteRequirementResultViewSet` 確認維持不需要獨立 `list`／`retrieve`（透過 `SupplierQuote` 詳情的 `items__requirement_results__requirement` 已可查詢，未發現獨立查詢需求），未另外補測試驗證此一設計判斷本身。

## 2026-09-01 收斂 DRAFT.md 兩個待決項目：purchase_suggestion.read 權限碼與 legacy service 移除
**現象**：前一輪盤點記錄了兩個待 Robin 決定的項目（`PurchaseSuggestionViewSet` 權限碼選擇、5 個已無引用的 legacy service 是否移除），Robin 一次核准兩項並要求直接執行，不要求分次確認。
**排查過程**：非除錯，屬已核准決策的直接落地。
**修復方式**：①`PurchaseSuggestionViewSet` 新增 `get_permissions()`：`list`／`retrieve` 改用 `HasPermissionCode`／`required_permission = "purchase_suggestion.read"`；`convert`／`dismiss` 維持 `IsAuthenticated`，授權仍交給 service 層既有的 `purchase_request.create`／admin 身分檢查，避免把「能看」與「能轉單／忽略」兩種能力混在同一個權限碼上（若直接對整個 ViewSet 套用單一 `HasPermissionCode`，會連帶擋下 `convert`／`dismiss`，這點在寫 RED 測試時被實際測出來，不是憑空推測）。`seed_demo_data.py` 的 `employee`／`admin` 角色補上這個新權限碼，`conftest.py` 的 `admin_user` fixture 同步補上。②確認 `approval_service.py`、`approval_routing_service.py`、`hallucination_check_service.py`、`quote_calculation_service.py`、`quote_summary_template.py` 及其專屬 legacy 測試檔（`test_approval_service.py`、`test_hallucination_check_service.py`、`test_quote_calculation_service.py`）無生產程式碼引用後移除；`tests/test_api_phase4.py` 移除對 `approval_routing_service.route_quote` 的測試資料建置依賴（該測試驗證的是 410 回應本身，不需要這段 setup）。
**根因**：不適用（本次是落地已核准的待決項目，非修復缺陷）。
**驗證方式**：新增 3 個測試（`test_purchase_suggestion_list_requires_dedicated_permission`、`test_purchase_suggestion_list_and_retrieve_visible_with_permission`、`test_purchase_suggestion_convert_and_dismiss_not_gated_by_read_permission`），先 RED 後 GREEN；移除 5 個 legacy service 後完整回歸 371→330 passed（減少的 41 個測試皆為被移除 service 的專屬單元測試），新增 3 個權限測試後 330→333 passed。Ruff、`makemigrations --check --dry-run`、`manage.py check` 皆通過。前端同步完成 `/quotes`→`/purchase-requests`、`QuoteListView.vue`→`PurchaseRequestListView.vue` 改名（`router/index.ts`、`navigation.ts`、`views/InquiryView.vue`、`tests/purchase-request-list.spec.ts` 一併更新），於裝置本機的 Linux VM 因 `node_modules` 是在 macOS 上安裝、`@rollup/rollup-linux-arm64-gnu` 缺失而無法直接執行 `vitest`／`vite build`，改為把 `frontend/src` 連同設定檔（不含 `node_modules`／`dist`）暫存進雲端環境、以 `pnpm install --frozen-lockfile` 重新安裝依賴後驗證：`vitest run` 25 passed、`vue-tsc -b` 無錯誤、`eslint .` 無錯誤、`vite build` 成功；裝置端與雲端驗證版本已用 SHA-256 逐檔比對一致。所有異動（含刪除／改名）已直接在裝置的實際檔案系統完成（`device_bash` 的沙箱不允許 `unlink`，改用同一檔案系統內的 `mv` 完成刪除與改名；`git mv`／`git add`／`git commit` 因 `.git/index.lock` 卡住的 stale lock 目前在該沙箱內無法寫入，不影響本次檔案異動本身，但代表 Robin 之後在自己機器上開一般終端機執行 `git add`／`git commit` 前，建議先確認沒有殘留的 `.git/index.lock`）。
**未驗證範圍**：`_to_delete_git_lock/`、`_to_delete_legacy_services/`、`_to_delete_tmp_sync/` 三個資料夾目前仍留在專案根目錄（`device_bash` 沙箱無法真正刪除檔案），內容分別是「移出的 stale git lock」「移出的 5 個 legacy service 與其測試檔」「先前批次同步用的暫存檔」，皆已確認不影響現有程式碼或測試，但需要 Robin 自行刪除這三個資料夾清理專案根目錄。

## 2026-09-02 Codex 程式碼審查發現 Phase 5「正式完成」有 3 個實質破口，Claude 獨立驗證後確認並修復其中 2 個

**現象**：Codex 對已標記「Phase 5 正式完成」（2026-09-01）的程式碼重新審查，回報 3 個缺口：①`manual_review_service.decide_review()` 核准 `supplier_fuzzy_match` 案件後，仍呼叫 `N8N_RESUME_WEBHOOK_URL` 續傳，但續傳流程內部又會打回已回 410 的 legacy `quotes/calculate/`／`quotes/verify-hallucination/`；②`QuoteViewSet`／`ApprovalViewSet` 的 `get_queryset()` 仍用 `user.role.role == "admin"／"employee"` 字串比對決定可視範圍，且兩者 `permission_classes` 只有 `IsAuthenticated`，未掛任何 permission code；③`purchase_suggestion_service.dismiss()` 仍直接檢查 `user.role.role == "admin"`，違反 `docs/ADR/discuss/erp.md` 2026-08-28 已 accepted 的「忽略操作改由 `purchase_suggestion.dismiss` 權限控制，不綁定系統管理員角色」決策，且該權限碼在種子資料／測試 fixture 裡根本不存在。Codex 同時指出 `docs/reference/api.md` 三處與程式碼不符：第 877 行仍描述會呼叫已刪除的 `services/quote_summary_template.py`；第 61 行寫 Quote 撤回「待後續切換」、第 941 行描述成可成功執行並改狀態，但實際固定回 410 `legacy_command_retired`。

**排查過程**：逐項獨立驗證，不直接採信 Codex 的結論：①讀 `manual_review_service.py`／`inquiry_resume_service.py` 確認呼叫鏈；另外追了 `n8n/workflows/inquiry-flow.json` 的「Webhook 續傳詢價」完整節點連線（BFS 走過 `main` connections），確認 `Django 試算報價（建立 Quote，續傳）`／`幻覺驗證（續傳）` 兩個 httpRequest 節點的 URL 分別是 `/api/v1/quotes/calculate/`／`/api/v1/quotes/verify-hallucination/`，兩者皆已回 410，屬實。過程中另外發現一個 Codex 沒提到、範圍更大的問題：Django 新核心 `parse_purchase_request_candidate()`（`inquiries/parse/` 使用）呼叫的是 `settings.N8N_INQUIRY_PARSE_WEBHOOK_URL`（預設路徑 `webhook/purchase-request-candidate`），但 `n8n/workflows/inquiry-flow.json` 裡只有 `inquiry`／`inquiry/resume` 兩個 webhook 節點，完全沒有 `purchase-request-candidate` 這個路徑——代表這個檔案本質上是 Phase 4.1／5 之前的舊版 n8n workflow，新核心實際依賴的「v2 純解析」webhook 從未匯出進版控，續傳流程要修，必須先確認要接去哪裡，不是單純換個 URL。②讀 `procurement/views.py` 對應行號確認屬實，且找不到任何 ADR 把「legacy 歷史查詢沿用角色字串」正式核准過，只有 `api.md` 自己這樣寫。③讀 `purchase_suggestion_service.py` 第 94 行確認屬實，比對 `docs/ADR/discuss/erp.md` 確認是違反已 accepted 決策；`grep` 種子資料／`conftest.py`／既有測試確認 `purchase_suggestion.dismiss` 這個權限碼完全不存在。文件矛盾三處逐一核對程式碼與 `ls`／`grep` 結果，確認皆屬實。同時發現我自己 2026-09-01「Phase 5 正式標記完成」的判定漏掉了①（只驗證了 Django 直接端點回 410，沒有追進 n8n 續傳分支內部的呼叫）與②（只確認了「有 permission code」，沒發現這兩個 ViewSet 的 queryset 過濾邏輯本身還在用角色字串），這兩項應由我自己負責，不是 Codex 誤判。

**根因**：①Phase 5.0 把 Django 直接端點停用，但沒有同步檢查／更新 n8n 內部續傳分支呼叫的下游端點，且新核心的 v2 純解析 webhook 從未真正匯出進 repo，形成「Django 端已切新核心、n8n 端還在打舊端點」的斷層。②③這兩處在補「全域改用 permission code」時被遺漏，屬檢查覆蓋不完整，不是刻意保留的例外。

**修復方式**：本次先修復②③（純 Django／RBAC 變更，無外部系統相依，可直接落地）；①因為牽涉 Robin 自己維護的 n8n 實例與尚未匯出的 v2 workflow，屬需要 Robin 決定的架構問題，本輪不動 `n8n/workflows/inquiry-flow.json` 或猜測續傳流程的正確目標端點，待 Robin 答覆後再處理。
②`backend/api/core/permissions.py` 新增 `HasAnyPermissionCode`（OR 語意，呼應前端 `navigation.ts` 的 `anyPermissions`）；`QuoteViewSet`／`ApprovalViewSet` 改用 `get_permissions()`：`list`／`retrieve` 套用 `HasAnyPermissionCode`（`required_permissions` 分別為 `("audit.read", "approval.read_all", "purchase_request.read_own")`／`("audit.read", "approval.read_all")`），`get_queryset()` 依 `audit.read`（全部）＞`approval.read_all`（自己角色簽核的詢價＋本人）＞僅 `purchase_request.read_own`（只見本人）分層；已退役的 `withdraw`／`claim`／`decide` 這幾個 legacy 寫入 action 維持 `IsAuthenticated`、不套用這組唯讀 permission code——寫 RED 測試時實測到：若整個 ViewSet 一律套用讀取權限，會讓沒有讀取權限的使用者呼叫這些已退役 action 時被擋成 403「沒有權限」，而不是原本設計的 410「這個指令已停用」，兩種錯誤語意不同，後者才是正確訊號。
③`purchase_suggestion_service.dismiss()` 改呼叫 `user_has_permission(user, "purchase_suggestion.dismiss")`；`seed_demo_data.py` 的 `admin` 角色與 `tests/conftest.py` 的 `admin_user` fixture 補上這個新權限碼（保留 demo 帳號原有能力，只是不再靠角色字串硬編碼）。
文件：`docs/reference/api.md` 三處已同步更正（見本次 commit 的文件異動）。

**驗證方式**：新增 `backend/tests/test_phase5_legacy_permission_codes.py`（8 個測試，涵蓋「無任何相關權限被擋 403」「`purchase_request.read_own` 只見本人」「`approval.read_all` 見角色範圍」「`audit.read` 見全部」，以及 `dismiss()` 的權限碼拒絕／通過情境），先 RED（8 個全數依預期失敗：讀取範圍未鎖權限、`dismiss` 仍走角色字串）後 GREEN。回歸過程中發現並修正 3 個因權限模型改變而失真的既有測試：`test_api_phase4.py::test_employee_quote_list_only_contains_own_quotes`（改為顯式授予 `purchase_request.read_own`，不再依賴角色字串隱式推斷）、`test_api_phase4.py::test_requester_can_withdraw_through_api` 與 `test_phase5_legacy_command_cutover.py::test_legacy_approval_commands_are_retired_without_mutation`（兩者原本預期任何登入使用者呼叫已退役 action 都回 410，因新加的 permission_classes 誤擋成 403，修法見上方「已退役 action 維持 IsAuthenticated」）；後兩者的失真由 `get_permissions()` 依 action 分流解決，不需修改測試本身。完整 Backend 回歸 382→390 passed，`ruff check`、`makemigrations --check --dry-run`、`manage.py check` 皆通過。

**未驗證範圍**：①（人工複核核准 `supplier_fuzzy_match` 後接回 n8n 續傳、續傳打進已退役 legacy 端點）尚未修復，待 Robin 決定續傳流程的正確目標（改接尚未匯出的 v2 純解析 webhook，或改由 Django 直接呼叫 `parse_purchase_request_candidate()` 略過 n8n 這段迴圈，或其他方案）。`docs/specs/PROGRESS.md` 已將 2026-09-01「Phase 5 正式標記完成」標記撤回，本次②③修復後 Phase 5 仍不算完成，需等①一併解決並經 Robin 確認後才重新標記。Robin 尚未在真實瀏覽器／n8n 環境驗證 Quote／Approval 歷史查詢的新可視範圍。

## 2026-09-02 修復破口①：人工複核核准後改由 Django 直接重新解析並自動建立草稿，不再接回 n8n 續傳

**現象**：延續上一則記錄的破口①。`manual_review_service.decide_review()` 核准 `supplier_fuzzy_match` 案件後呼叫 `inquiry_resume_service.trigger_resume()`，該函式打 `N8N_RESUME_WEBHOOK_URL`；沿該 n8n workflow 的「Webhook 續傳詢價」節點往下追（BFS 走過 `connections` graph），兩個下游 httpRequest 節點打的是已回 410 的 legacy `/quotes/calculate/`／`/quotes/verify-hallucination/`，核准後其實無法真的把候選解析出來、也不會建立任何單據。另外，Django 新核心 `parse_purchase_request_candidate()`（`/inquiries/parse/` 使用）依賴的 `N8N_INQUIRY_PARSE_WEBHOOK_URL`（`webhook/purchase-request-candidate`）這個「v2 純解析」webhook，從未匯出進 `n8n/workflows/inquiry-flow.json`（該檔案只有 `inquiry`／`inquiry/resume` 兩個路徑），代表這個 webhook 目前只存在於 Robin 自己維護的 n8n 實例上。

**排查過程**：向 Robin 提出兩個需要架構決定的問題並取得明確答覆：(1) 續傳流程要改接去哪裡——Robin 選擇「Django 直接呼叫，略過 n8n」；(2) 重新解析出的結果要如何處理（原設計只回傳 `resume_triggered` 布林值，沒有任何地方讓使用者看到重新解析的候選）——Robin 選擇「自動建立採購需求草稿」，並指示解析失敗／欄位有缺漏時不建草稿、回退成 `resume_triggered=false` 交管理員人工確認。依此設計實作，走 TDD RED→GREEN。

**根因**：見上一則記錄——Phase 5.0 停用 Django 直接端點時沒有同步處理 n8n 續傳分支內部呼叫的下游端點；且新核心的 v2 純解析 webhook 從未真正匯出進版控，形成「Django 端已切新核心、n8n 端還在打舊端點」的斷層，這裡是該斷層的最後一段修復。

**修復方式**：
1. `backend/services/masking_service.py`：`_best_match()` 除了原本的 `(best_ratio, best_match_len)` 外，新增回傳比對到的片段位置 `best_span`（`(start, end)`，找不到候選視窗時為 `None`），供精確替換原文用；`_find_fuzzy_supplier_candidates()` 同步調整解包（忽略新增的第三個值，行為不變）。新增 `mask_confirmed_supplier_text(raw_text, supplier)`：供應商已由人工確認，不重新跑模糊比對（避免又繞回複核佇列造成無限迴圈）——優先找精確子字串命中直接遮罩，找不到才用 `_best_match` 定位的最相似片段位置做替換，其餘仍照 `_mask_amounts()` 規則遮罩金額。
2. `backend/services/inquiry_service.py`：重構 `parse_purchase_request_candidate()`，抽出 `_request_and_unmask_candidate()`（呼叫 n8n 候選解析＋還原遮罩）、`_resolve_supplier_candidates()`、`_resolve_item_candidates()` 三個共用 helper（純提取，行為不變，既有測試全數通過驗證）。新增 `resolve_candidate_after_manual_review(raw_text, *, supplier, requester_id)`：供應商已確定（不需要再解析 `supplier_candidates`），改用 `mask_confirmed_supplier_text` 遮罩後呼叫既有的候選解析 n8n webhook（與 `/inquiries/parse/` 共用同一個 v2 端點），品項解析與回填邏輯共用 `_resolve_item_candidates()`。
3. `backend/services/inquiry_resume_service.py`：整份改寫（不再呼叫 `requests.post`／`N8N_RESUME_WEBHOOK_URL`）。`trigger_resume()` 改為：查回已確認的 `Supplier`／原始發起人 `User`（查無則拋 `InquiryResumeError`，屬資料完整性問題）→ 呼叫 `resolve_candidate_after_manual_review()`（解析失敗或原始輸入有問題則回傳 `None`）→ `ready_for_draft` 為否則回傳 `None`（不建草稿，讓管理員人工確認）→ 呼叫既有的 `purchase_request_draft_service.create_draft()` 建立草稿（`DraftError`——例如發起人沒有 `purchase_request.create` 權限——同樣視為「無法自動建立」回傳 `None`，不讓 API 500）→ 成功則把 `source` 標記為 `manual_review_resume`（比照 `purchase_suggestion_service.convert_to_draft()` 用 `source="inventory_suggestion"` 的既有慣例）並回傳建立好的 `PurchaseRequest`。
4. `backend/services/manual_review_service.py`：`decide_review()` 依 `trigger_resume()` 的回傳值（`PurchaseRequest` 或 `None`）設定 `review.resume_triggered`（`draft is not None`）與 `review.created_purchase_request_id`（成功時才設定；兩者皆為 in-memory-only 屬性，不落地 DB，語意與既有 `resume_triggered` 一致）。頂部與 `_decide_supplier_fuzzy_match()` 內的說明註解同步更新，不再描述「交還 n8n」。
5. `backend/api/audit/views.py`：`decide` action 的 response 新增 `created_purchase_request_id`（`resume_triggered` 為真時才帶出），供前端導去該筆自動建立的採購需求草稿。

`settings.N8N_RESUME_WEBHOOK_URL`（`config/settings.py`）與 `n8n/workflows/inquiry-flow.json` 的「Webhook 續傳詢價」分支，本次修復後在 Django 端已無任何呼叫者，屬死設定／死分支，本次未刪除（非本次需求直接涉及範圍，且刪除 n8n workflow JSON 屬 Robin 自行維護的範圍），僅在此記錄供之後清理參考。

**驗證方式**：TDD RED→GREEN，新增／調整測試：`tests/test_masking_service.py` 新增 6 個測試（`mask_confirmed_supplier_text` 的精確命中、模糊 typo 命中、完全無相似片段、unmask 還原、空值、supplier 為 None）；`tests/test_inquiry_service.py` 新增 3 個測試（`resolve_candidate_after_manual_review` 成功回填、品項缺漏不 ready、空輸入拋錯），既有 21 個測試全數維持通過（確認重構未改變 `parse_purchase_request_candidate()` 行為）；整份改寫 `tests/test_inquiry_resume_service.py`（8 個測試，涵蓋成功建立草稿、欄位缺漏不建立、AI 解析失敗、輸入驗證失敗、供應商／發起人查無資料、發起人無建立權限）；`tests/test_manual_review_service.py`（17 個既有測試在 mock `trigger_resume` 的層級不受影響，全數維持通過，未修改）；`tests/test_api_phase3.py::test_decide_action_approved_resumes_candidate_parse` 更新為驗證 response 帶出 `created_purchase_request_id`。完整 Backend 回歸 390→404 passed，`ruff check .`、`python manage.py makemigrations --check --dry-run`（No changes detected）、`python manage.py check`（0 issues）皆通過。已用 SHA-256 雙邊校驗同步回裝置。

**未驗證範圍**：Robin 尚未在真實瀏覽器／n8n 環境走一次「輸入含打錯字的供應商名稱→模糊比對進複核佇列→管理員核准→自動建立採購需求草稿」的完整端到端流程；`resolve_candidate_after_manual_review()` 呼叫的 v2 純解析 webhook（`N8N_INQUIRY_PARSE_WEBHOOK_URL`）本身的正確性沿用既有 `/inquiries/parse/` 流程已驗證過的行為，未針對「供應商已知、只解析品項」這個較窄的呼叫情境另外用真實 n8n 驗證過。Phase 5 三個破口（①②③）皆已修復，但「正式完成」狀態是否重新標記，待 Robin 確認後再更新 `docs/specs/PROGRESS.md`，不由 Claude 自行標記。

## 2026-09-02 Codex 複查上一則①的修復，發現 4 項缺口，Claude 獨立驗證後確認全部屬實並修復其中 2 項

**現象**：Codex 對上一則記錄的①修復（Django 直接呼叫，略過 n8n）重新審查，回報 4 項缺口：(1) 並未「完全略過 n8n」——`resolve_candidate_after_manual_review()` 仍會呼叫 n8n v2 pure-parse webhook 解析品項；(2) 續傳失敗後沒有可追蹤、可重試機制——`resume_triggered`／`created_purchase_request_id` 只是 transient 欄位，`decide_review()` 把案件標成 `resolved` 的交易在呼叫續傳邏輯之前就已提交，失敗時管理員重新整理後無法得知草稿有沒有建立，也無法重試；(3) 正式文件未同步——`SPEC.md` FR-6a 沒寫「核准後自動建立草稿」、`docs/ADR/discuss/main-flow.md` 舊的 accepted 決策沒標記 superseded、`docs/reference/deploy.md` 仍把 `N8N_RESUME_WEBHOOK_URL` 列為必要變數且描述舊行為、`config/settings.py` 註解也是舊行為；(4) `masking_service.mask_confirmed_supplier_text()` 找不到可定位的供應商片段時會 fail-open（保留原文只遮罩金額），與 NFR-1「送往 LLM 的內容必須先脫敏」沒有例外的要求不一致。

**排查過程**：逐項獨立驗證，不直接採信 Codex 的結論：(1) 讀 `inquiry_service.py` 202／223 行與 `repositories/inquiry.py` 8 行確認屬實——但這是設計本身如此，不是漏做：Django 沒有能力自己做 NLP，「略過 n8n」原本的決策範圍是「不再讓 n8n 負責續傳編排／不再打已退役端點」，不是「完全不需要任何外部 LLM 呼叫」，這點在上一則記錄裡已寫清楚，只是 PROGRESS.md／對 Robin 的口頭摘要用了「略過 n8n」這種簡化措辭，容易被誤讀成完全不碰 n8n。(2) 讀 `manual_review_service.py` 65／79 行確認：`review.status = RESOLVED` 確實在 `transaction.atomic()` 區塊內、`trigger_resume()` 呼叫之前就已提交；`resume_triggered`／`created_purchase_request_id` 確認只設在 Python 物件上、從未 `.save()`，重新查詢 DB 會遺失，屬實。(3) 逐一開檔核對：`SPEC.md` 第 83 行（FR-6a）、`docs/ADR/discuss/main-flow.md` 第 179～203 行（2026-08-27 條目仍是 accepted）、`docs/reference/deploy.md` 第 20／41 行、`backend/config/settings.py` 第 117～119 行，皆與現況不符，屬實。(4) 讀 `masking_service.py` 第 140 行確認：`start is None` 分支確實只遮罩金額、把供應商全名原文保留在送往 n8n 的文字裡，且 `docs/specs/SPEC.md` NFR-1 明文「送往 LLM 的內容必須先脫敏」沒有但書，這個 fallback 違反已定案規格，屬實，且是本次自己寫的程式碼，非既有缺陷。

**根因**：(1) 屬措辭不精確造成的認知落差，非程式邏輯錯誤。(2) 屬既有設計（Phase 3 起就是「DB 交易與外部通知分開，外部失敗不回滾」）在「外部通知」變成「外部通知＋自動建立正式單據」後，原本可接受的「transient 欄位、無需重試」設計已不再足夠，風險層級隨行為改變而提高，但撰寫①修復時沒有重新評估這個既有設計是否仍然適用。(3) 屬於把「架構決策」誤當「純程式修復」處理——只寫了 debug ADR，沒有依 AGENTS.md「討論過程即時記進 discuss ADR」的規則同步 discuss ADR、SPEC.md 與 reference。(4) 屬撰寫①修復時的判斷疏漏：選擇了「不確定就保守降級（只遮罩金額）」而非「不確定就中止（fail-closed）」，沒有意識到這與 NFR-1 的字面規定衝突。

**修復方式**：本次直接修復 (3)(4)，(1) 屬澄清、不需要改程式碼，(2) 因涉及 Migration（`manual_review_queue` 新增落地欄位）與新增受權限控制的重試 API，依 AGENTS.md 需先提出影響範圍、資料轉換與回滾方案並取得 Robin 核准，已記錄進 `docs/specs/DRAFT.md`「待討論」，本次不逕自實作。
(4) `backend/services/masking_service.py`：`mask_confirmed_supplier_text()` 找不到可定位片段時改為 `raise MaskingError(...)`，不再 fail-open。`backend/services/inquiry_service.py`：`resolve_candidate_after_manual_review()` 新增 `except MaskingError` 轉成 `InquiryValidationError`（呼叫端 `trigger_resume()` 已知會攔截的例外型別），確保這個例外不會在 `decide_review()` 的 DB 交易已提交之後未被攔截、把 API 炸成 500。
(3) `docs/ADR/discuss/main-flow.md`：2026-08-27「供應商模糊比對案件核准後，交還 n8n 流程的通知機制」條目標記 `superseded`，新增 2026-09-02 條目記錄新決策（Django 直接接管續傳編排、仍呼叫既有 v2 純解析 webhook 完成 AI 解析、①②④三項後果，含 (2) 的已知缺口待討論說明）。`docs/specs/SPEC.md` FR-6a 補上「核准後自動建立草稿／解析失敗或缺漏不建草稿」的行為描述並連結新 ADR 條目。`docs/reference/deploy.md`：Django 必要環境變數移除 `N8N_RESUME_WEBHOOK_URL`（改註記「有預設值但現況已無呼叫者」）；n8n Webhook 端點說明改為如實描述續傳分支已是死分支。`backend/config/settings.py`：`N8N_RESUME_WEBHOOK_URL` 上方註解改為如實描述現況（保留設定與預設值只是避免尚未更新 `.env` 的環境啟動失敗）。

**驗證方式**：TDD RED→GREEN：`tests/test_masking_service.py::test_mask_confirmed_supplier_text_unlocatable_span_raises_instead_of_leaking_name` 取代原本斷言「fail-open 只遮罩金額」的測試，改斷言 `pytest.raises(MaskingError)`；`tests/test_inquiry_resume_service.py` 新增 `test_trigger_resume_unmaskable_supplier_returns_none_without_leaking_name`——端到端（不 mock `resolve_candidate_after_manual_review`）驗證 `trigger_resume()` 在這個情境下正確回傳 `None`、不建立草稿，也不會讓例外往上炸穿已提交的複核決議交易。`tests/test_masking_service.py`／`tests/test_inquiry_service.py`／`tests/test_inquiry_resume_service.py` 三個測試檔完整跑過 61 passed。

**未驗證範圍**：(2)（持久化續傳狀態與重試機制）尚未實作，待 Robin 就 `docs/specs/DRAFT.md` 待討論項目給出決定與 Migration 核准；(1) 的措辭澄清已在本則與 `docs/ADR/discuss/main-flow.md` 新條目中寫清楚，但尚未再次向 Robin 口頭確認「Django 直接接管續傳編排＋仍需 n8n 做 AI 解析」是否就是他原本想要的設計。Phase 5「正式完成」狀態維持撤回，不由 Claude 自行重新標記。

## 2026-09-02 實作破口②：新增持久化續傳狀態與重試 API（Migration）

**現象**：延續上一則記錄的破口②。Robin 選擇「現在就做」，要求先提出 Migration 方案（影響範圍／資料轉換／鎖表風險／回滾方案）取得核准後才實作；核准內容見 `docs/ADR/discuss/main-flow.md` 2026-09-02「持久化續傳狀態與重試」條目，Robin 一併核准同步修正過時的前端 `ManualReviewView.vue`。

**排查過程**：不適用（本則為既定方案的實作，非排查性質）。

**根因**：見上一則記錄的根因 (2)：`resume_triggered`／`created_purchase_request_id` 只存在於單次 request 記憶體中，決議 DB 交易早於續傳邏輯提交，續傳失敗時無持久化狀態可查也無法重試。

**修復方式**：
1. `backend/apps/audit/models.py`：`ManualReviewQueue` 新增 `ResumeStatus` TextChoices（`not_applicable`／`pending`／`succeeded`／`failed`，預設 `not_applicable`）、`resume_status`、`resume_error_code`（非敏感錯誤代碼，`max_length=40`，nullable）、`created_purchase_request`（FK → `apps.procurement.models.PurchaseRequest`，`on_delete=SET_NULL`，nullable）三個欄位，`Meta.constraints` 新增 2 個 `CheckConstraint`（`succeeded` 必有 `created_purchase_request_id`、`failed` 必有 `resume_error_code`）。產生 Migration `apps/audit/migrations/0004_manualreviewqueue_created_purchase_request_and_more.py`（純新增欄位＋約束，皆有預設值或允許 null，不需要資料回填既有案件；`makemigrations --check --dry-run` 確認無殘留未產生的變更）。
2. `backend/services/inquiry_service.py`：新增 `InquiryUnmaskableSupplierError(InquiryValidationError)`；`resolve_candidate_after_manual_review()` 捕捉 `MaskingError` 時改拋這個專屬子類別（原本拋的是一般 `InquiryValidationError`），讓「供應商片段找不到」對應到獨立的 `resume_error_code=unmaskable_supplier`。
3. `backend/services/inquiry_resume_service.py`：`trigger_resume()` 回傳值從 `PurchaseRequest | None` 改為 `(draft, error_code)` tuple；新增 `RESUME_ERROR_*` 模組級常數（`invalid_input`／`unmaskable_supplier`／`parse_failed`／`missing_fields`／`permission_denied`／`draft_creation_failed`／`resume_data_error`，皆不含原始例外訊息或供應商名稱），依例外階層由細到粗依序攔截對應到各錯誤代碼；`create_draft()` 拋出的 `DraftPermissionDenied`（`DraftError` 子類別）單獨對應 `permission_denied`，其餘 `DraftError` 對應 `draft_creation_failed`。
4. `backend/services/manual_review_service.py`：`decide_review()` 在既有 `transaction.atomic()` 區塊內，核准 `supplier_fuzzy_match` 時多存一個 `resume_status=pending`（與 `status`／`decision` 同一次 `save()` 提交，避免多一次資料庫往返）；交易提交後才呼叫抽出的 `_execute_resume_and_persist()`，依 `trigger_resume()` 的回傳值做第二次 `save()` 落地最終 `resume_status`／`resume_error_code`／`created_purchase_request`（`InquiryResumeError` 對應 `resume_data_error`）。新增 `retry_resume(review_id, user_id)`：需要 `manual_review.decide` 權限；用 `select_for_update()` 鎖列驗證「必須是已核准的 `supplier_fuzzy_match` 案件」（否則 `ManualReviewError`／400）與「必須是 `resume_status=failed`」（否則 `ManualReviewConflictError`／409），flip 成 `pending` 並提交交易釋放鎖，才在交易外呼叫 `_execute_resume_and_persist()`（沿用 `decide_review()`「外部呼叫不佔用 DB 鎖」的既有設計），並寫入一筆 `action_type="review_resume_retry"` 的稽核 log。
5. `backend/schemas/audit.py`：`ManualReviewQueueSerializer` 新增 `resume_status`／`resume_error_code`／`created_purchase_request` 三個真實欄位。`backend/api/audit/views.py`：新增 `POST .../retry-resume/`（`url_path="retry-resume"`，沿用既有 `get_permissions()` 的 `manual_review.decide` 分流）；`decide`／`claim`／`retry` 三個 action 共用新增的 `_resume_conflict_or_error_response()` helper 統一把 `LegacyManualReviewRetiredError`／`ManualReviewConflictError`／其餘 `ManualReviewError` 轉成 410／409／400，移除原本 `decide` action 拼裝 transient 欄位的 `hasattr`/`getattr` 寫法（serializer 已直接輸出真實欄位）。
6. `frontend/src/types/api.ts`：`ManualReview` 新增 `resume_status`／`resume_error_code`／`created_purchase_request` 型別；`frontend/src/views/ManualReviewView.vue`：`decide()` 改判斷 `resume_status === 'failed'`（移除過時的「n8n 續傳通知失敗」文案），新增 `retryResume()` 呼叫新端點，`resume_status=failed` 時顯示「重試續傳」按鈕。
7. 附帶修復（與本次功能無直接關聯，但在驗證過程中發現、屬於阻擋測試執行的環境性 bug，見下一則記錄）：`crm/0002`、`erp/0002` migration 缺少對 `core/0003`（建立 `set_row_updated_at()` trigger function 的遷移）的依賴宣告，從全新資料庫 `migrate` 時可能因拓樸排序把它們排到 `core/0003` 之前執行而失敗。

**驗證方式**：TDD RED→GREEN。新增／調整測試：`tests/test_manual_review_service.py` 新增 3 個 CheckConstraint 測試（`succeeded` 缺草稿 id、`failed` 缺錯誤代碼皆觸發 `IntegrityError`；`failed` 帶錯誤代碼合法）與 6 個 `retry_resume()` 測試（成功重試、狀態不符 409、非 `supplier_fuzzy_match`／未核准 400、案件不存在 400、非管理員 400），既有 `decide` 相關測試改用真實 `PurchaseRequest` 實例斷言 `resume_status`／`resume_error_code`／`created_purchase_request_id`；`tests/test_inquiry_service.py` 新增 `InquiryUnmaskableSupplierError` 單元測試；`tests/test_inquiry_resume_service.py` 全數改為斷言 `(draft, error_code)` tuple；`tests/test_api_phase3.py` 新增 3 個 `retry-resume` API 測試，`decide` 測試改斷言真實回應欄位。完整 Backend 回歸從乾淨資料庫（見下一則記錄的環境修復）跑出 419 passed，`ruff check .`、`python manage.py makemigrations --check --dry-run`（No changes detected）、`python manage.py check`（0 issues）、`git diff --check` 皆通過。前端：`vue-tsc -b`、`eslint .`、`vitest run`（22 個測試檔、95 個測試全數通過，`ManualReviewView.vue` 本身目前沒有既有的元件測試，非本次新增缺口）、`vite build` 皆通過。已用 SHA-256 雙邊校驗同步回裝置。

**未驗證範圍**：併發重試（兩位管理員同時對同一失敗案件呼叫 `retry-resume`）僅靠 `select_for_update()` 鎖列防護，未寫多執行緒／多連線測試實際驗證第二個請求會被 409 擋下；Robin 尚未在真實瀏覽器環境操作過「核准→續傳失敗→按重試續傳→成功」的完整端到端流程。`resume_error_code` 的合法值集合固定為 7 個字串常數，未來若新增失敗類型需同步更新 `docs/reference/api.md` 的列舉。Phase 5「正式完成」狀態維持撤回，不由 Claude 自行重新標記。

## 2026-09-02 環境修復：crm／erp migration 缺少對 core 的依賴宣告，全新資料庫 migrate 會失敗

**現象**：與上一則記錄的功能無關，是驗證上一則 Migration 時意外發現的獨立問題。從全新（未套用任何 migration）的 PostgreSQL 資料庫執行 `python manage.py migrate` 或 `pytest`（會自動建立全新的 `test_groundtruth`），在套用 `crm/0002_supplier_code_supplier_contact_supplier_is_active_and_more` 時噴 `django.db.utils.ProgrammingError: function set_row_updated_at() does not exist`。

**排查過程**：`grep` 全部 migration 檔案找出誰呼叫 `set_row_updated_at()`、誰定義它：函式本身由 `core/0003_permission_rolepermission_userrole.py` 的 `RunSQL` 建立；`crm/0002_*.py`、`erp/0002_*.py` 的 `RunSQL` 都會建立使用這個函式的 trigger，但兩者的 `dependencies` 都只寫了同 app 內的上一個 migration（`('crm', '0001_initial')`／`('erp', '0001_initial')`），沒有宣告對 `core/0003` 的依賴；`procurement/0004_*.py` 則正確宣告了這個依賴（可能是因為它同時也需要 `core` 的其他 model）。從乾淨資料庫執行 `showmigrations` 確認：Django 的拓樸排序在套用完 `core/0001`、`core/0002` 後，因為沒有相依邊強制順序，選擇先執行 `crm/0002`（早於 `core/0003`），因而觸發此錯誤；手動先跑 `python manage.py migrate core` 再跑 `python manage.py migrate` 可以繞過，證實這是純粹的 migration 依賴圖排序問題，不是 SQL 本身有誤。

**根因**：`crm/0002`、`erp/0002` 撰寫時遺漏宣告對 `core/0003`（建立共用 trigger function 的 migration）的跨 app 依賴。這個問題此前一直沒有被發現，是因為這個 session 的本地開發資料庫與 `test_groundtruth` 測試資料庫長期沿用同一份、早期用某種能成功的順序建立過一次的既有資料庫（pytest-django 預設重用既有測試資料庫、不會每次都從乾淨狀態重建），從未真正從空資料庫走過一次完整 `migrate`；本次因故重建本地資料庫才第一次暴露。

**修復方式**：`backend/apps/crm/migrations/0002_supplier_code_supplier_contact_supplier_is_active_and_more.py`、`backend/apps/erp/migrations/0002_productcategory_product_description_and_more.py`：`dependencies` 各自新增 `('core', '0003_permission_rolepermission_userrole')`。只新增依賴宣告，不改動任何 `operations`／SQL，因此對已經套用過這兩個 migration 的既有資料庫（含 Robin 的裝置）沒有影響——Django 依 migration 名稱判斷是否已套用，不會因為新增依賴宣告而重新執行。

**驗證方式**：`DROP DATABASE`／`CREATE DATABASE` 重建全新本地資料庫與 `test_groundtruth`，`python manage.py migrate` 一次到底不再需要手動分段，全數 `OK`；`pytest -q` 從乾淨狀態跑出 419 passed（含上一則記錄新增的測試）。

**未驗證範圍**：未逐一排查是否還有其他 migration 存在類似遺漏依賴的情況（本次只針對觸發實際錯誤的 `set_row_updated_at()` 使用者做了排查），若之後在其他情境下重建乾淨資料庫又遇到類似錯誤，屬於同一類問題，可比照本次方式排查修復。
