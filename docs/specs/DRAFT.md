---
updated: 2026-08-27
---

# 未定案草稿

> 想法先記在這裡，使用者確認要做才依 SPEC-TEMPLATE.md 的功能區塊格式升級進 `docs/specs/SPEC.md`。

## 待討論

- [ ] YYYY-MM-DD：<點子描述>

## 已取消

- YYYY-MM-DD：<點子描述> — 取消原因：

## 擱置中

- 2026-08-24：待人工複核佇列的逾期自動升級通知機制（例如超過 4 小時未處理再發提醒信、升級通知更高層級） — 擱置原因：已改用「通知所有管理員 + 認領機制」解決單點失聯風險，逾期升級需要額外排程/計時器基礎設施，複雜度超出目前「深度夠用即可」的範圍，若之後有時間可再評估。詳見 `docs/ADR/discuss/main-flow.md`。
- 2026-08-24：把系統部署到 Render/Vercel 等雲端平台，讓面試官可直接連上網址互動 — 擱置原因：這是模擬情境的展示專案，非正式上線，Docker Compose 本地一鍵啟動＋demo 影片/截圖已足夠展示；公開部署會增加環境變數、跨服務網路、免費方案休眠等維運成本，且會讓 LLM API Key、Gmail OAuth、webhook 端點暴露在公開網路上，風險與效益不成比例。若之後想要面試時能讓對方即時互動，可再評估。詳見 `docs/ADR/discuss/main-flow.md`。
- 2026-08-27：AI 模型部署方式選用（地端／私有雲／公開雲端 API）的判斷準則 — 擱置原因：目前 Phase 3 用遮罩層（供應商/金額 Token 化後才送 Gemini）搭配幻覺驗證，已足以在使用公開雲端 LLM API 的前提下處理資料安全與輸出可信度，這是模擬展示專案，不需要地端模型的額外硬體與部署複雜度。若之後要正式評估，判斷準則大致是：①公開雲端 API（目前採用）——資料離開內網前已用固定程式邏輯遮罩敏感欄位，且無法規/產業規範禁止資料出境時適用，開發與維運成本最低；②私有雲（同雲端供應商的 VPC/專屬租戶部署，例如 GCP 私有端點）——資料完全不遮罩也要送給 LLM、但公司資安政策允許資料留在指定雲端環境（不能是任何人都能存取的公開端點）時適用，成本與複雜度中等；③純地端模型（GPU 自架）——資料完全不能離開內網（金融、醫療等受法規約束產業），或有極端資安要求時才需要，需重新評估整個技術棧（Prompt 設計、效能調校、模型能力都要重做一輪）。若之後有時間可再評估。
- YYYY-MM-DD：<點子描述> — 擱置原因：
